# OpenMir2 Rust现代化引擎 技术文档 V4.0

**文档编号**：OM2-TD-2026-V4.0
**版本状态**：归档版
**编制目的**：基于传统8服务架构 + Bevy客户端 + ECS服务端引擎的技术方案
**核心定位**：服务端与客户端均采用Rust生态，共享技术栈，全 ECS 架构

---

## 文档概述

本文档定义的引擎架构延续传统Mir2传奇引擎的**8服务经典架构**，同时在技术实现层面做出两项根本性升级：

第一，客户端引擎选用**Bevy**（Rust原生游戏引擎），而非传统C++/C#引擎，实现服务端与客户端的**技术栈统一**，代码复用率最大化。

第二，M2Server主引擎采用**ECS（Entity Component System）架构**设计，将游戏实体、属性、行为彻底解耦，告别传统面向对象继承带来的耦合困境，实现高性能数据导向的游戏逻辑。

第三，职业体系严格遵循**传奇经典三职业**（战士/法师/道士），属性体系采用**传奇经典属性面板**（HP/MP/AC/MAC/DC/MC/SC等），玩法100%还原。

**核心技术栈**：Rust + tokio + Bevy（客户端）+ ECS（服务端核心逻辑）+ egui（服务端界面）+ MySQL/Redis

---

## 第一章：核心技术架构总览

### 1.1 整体技术架构图

```
┌────────────────────────────────────────────────────────────────────┐
│                         运营人员（egui管理界面）                        │
│                   每个服务独立egui窗口 / GameCenter总控               │
└────────────────────────────────────────────────────────────────────┘
                    │                    │
     ┌───────────────┴───────┐            │
     ▼                       ▼            ▼
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│LoginGate│ │ SelGate │ │ RunGate │ │LoginSrv │ │DBServer │
│ Rust    │ │ Rust    │ │ Rust    │ │ Rust    │ │ Rust    │
│ egui    │ │ egui    │ │ egui    │ │ egui    │ │ egui    │
└─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘
                                    │            │
                                    │            │
                ┌───────────────────┴────────────┘
                ▼
┌─────────────────────────────────────────────────────────────┐
│                        M2Server                              │
│                     Rust + ECS + tokio                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    ECS World                          │  │
│  │  Entities: 玩家 / 怪物 / NPC / 掉落物 / 技能特效         │  │
│  │  Components: 位置 / HP/MP / 属性 / 装备 / 技能 / AI状态  │  │
│  │  Systems: 移动系统 / 战斗系统 / AI系统 / 技能系统        │  │
│  └──────────────────────────────────────────────────────┘  │
│                        egui管理界面                            │
└─────────────────────────────────────────────────────────────┘
                                    │
                ┌───────────────────┴───────────────────┐
                ▼                                       ▼
┌─────────────────────────┐                 ┌─────────────────────────┐
│     LogServer           │                 │     GameCenter          │
│     Rust + egui         │                 │     Rust + egui         │
└─────────────────────────┘                 └─────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        客户端（Bevy）                          │
│  ┌───────────────┐  ┌─────────────┐  ┌────────────────────┐  │
│  │ 渲染引擎      │  │ 2D精灵系统  │  │  网络通信模块       │  │
│  │ (wgpu)        │  │ 动画系统    │  │  (tokio+tokio-tungstenite) │
│  └───────────────┘  └─────────────┘  └────────────────────┘  │
│  ┌───────────────┐  ┌─────────────┐  ┌────────────────────┐  │
│  │ UI系统        │  │ 音频系统    │  │  输入系统          │  │
│  │ (bevy_ui)     │  │ ( kira )   │  │  (键盘/鼠标/触摸)  │  │
│  └───────────────┘  └─────────────┘  └────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
         ▲                                    │
         │ TCP Binary Protocol                │
         │ (Protobuf序列化)                   │
         └────────────────────────────────────┘
```

### 1.2 技术栈选型理由

| 技术选型 | 选型理由 |
|---------|---------|
| **Rust**（服务端+客户端统一） | 内存安全、高性能、无GC停顿、编译期消除数据竞争；服务端和客户端共享数据结构定义代码 |
| **tokio** | Rust生态最成熟的异步运行时，服务端网络层和客户端网络层共用 |
| **Bevy** | 纯Rust游戏引擎，wgpu原生渲染，ECS架构与M2Server的ECS架构同构；社区活跃、2D支持优秀、无C++依赖 |
| **ECS架构**（M2Server） | 数据与行为分离、缓存友好、并行处理简单；游戏实体（玩家/怪物/NPC）天然适合ECS模型 |
| **egui**（服务端界面） | 纯Rust即时模式GUI，与服务端程序同进程运行；美观、现代、跨平台 |
| **Lua**（脚本引擎） | 游戏业务逻辑脚本化，热更新不停服；通过mlua与Rust集成 |
| **MySQL + Redis** | 运营团队熟悉，稳定性高，生态成熟 |
| **Protobuf**（客户端-服务端协议） | 跨语言支持好（Rust/C++）、序列化高效、版本兼容能力强 |

### 1.3 8服务架构继承自V3.0

V4.0完全继承V3.0定义的8服务架构，包括服务职责划分、服务间通信协议、egui管理界面设计、部署方案等基础内容均保持不变。V4.0的变更聚焦于以下三个维度：

| 变更维度 | V3.0 | V4.0 |
|---------|------|------|
| 客户端引擎 | Unity | **Bevy** |
| M2Server架构 | 面向对象模块化 | **ECS架构** |
| 职业体系 | 未明确定义 | **战/法/道三职业** |
| 属性体系 | 通用MMO属性 | **传奇经典属性** |

---

## 第二章：Bevy客户端架构设计

### 2.1 为什么选择Bevy

传统游戏客户端开发通常选择Unity/Unreal/Cocos等引擎，但选择Bevy具有以下独特优势：

| 维度 | Unity | Unreal | Cocos | **Bevy** |
|------|-------|--------|-------|----------|
| 语言 | C# | C++ | C++/Lua | **Rust** |
| 引擎代码可读性 | 中 | 差 | 中 | **优** |
| 内存安全 | 托管GC | 手动管理 | 手动管理 | **编译期安全** |
| 服务端代码复用 | 无 | 无 | 无 | **可共享结构定义** |
| 2D游戏支持 | 优 | 一般 | **优秀** | **优** |
| 社区生态 | 成熟 | 成熟 | 成熟 | **成长中** |
| 商业案例 | 大量 | 3A游戏 | 手游为主 | **初期** |
| 学习曲线 | 低 | 高 | 中 | **中高** |

**Bevy的核心优势**：

第一，**技术栈统一**。服务端Rust代码中的数据结构（如PlayerData、ItemData、SkillData）可以直接在客户端项目中引用，无需维护两套定义，避免协议不一致问题。

第二，**ECS架构同构**。M2Server使用ECS架构处理服务端游戏逻辑，Bevy原生使用ECS架构（bevy_ecs），客户端和服务端使用相同的ECS概念，团队只需要理解一套架构。

第三，**原生wgpu渲染**。Bevy基于wgpu实现渲染，不依赖DirectX/OpenGL/Vulkan的特定绑定，天然支持多平台（Windows/Linux/macOS/Web/移动端规划中）。

第四，**开源可控**。Bevy完全开源，协议宽松（MIT/Apache-2.0），无授权费用，无商业引擎锁定风险。

### 2.2 客户端架构分层

```
┌─────────────────────────────────────────────────────┐
│                    游戏入口层                         │
│              (main.rs, Bevy App启动)                 │
└─────────────────────┬───────────────────────────────┘
                      │
         ┌────────────┼────────────┐
         ▼            ▼            ▼
┌──────────────┐ ┌──────────┐ ┌──────────────┐
│  渲染模块    │ │  逻辑模块 │ │  网络模块    │
│  (bevy_render)│ │ (Custom) │ │  (tokio)    │
├──────────────┤ ├──────────┤ ├──────────────┤
│  2D精灵渲染  │ │ ECS World│ │ 协议收发    │
│  瓦片地图渲染│ │ 游戏逻辑 │ │ 连接管理    │
│  特效渲染   │ │ 动画状态 │ │ 重连机制    │
│  UI渲染     │ │ 音效触发 │ │ 心跳检测    │
│  (bevy_sprite│ │          │ │              │
│   bevy_ui)  │ │          │ │              │
└──────────────┘ └──────────┘ └──────────────┘
```

### 2.3 核心模块设计

#### 2.3.1 瓦片地图渲染模块

传奇类游戏的地图由瓦片（Tilemap）构成，客户端需要高效渲染大规模瓦片地图。

**技术实现**：

- 使用 **bevy_ecs_tilemap** 插件处理瓦片渲染
- 地图数据以Chunks为单位分块加载（每个Chunk = 16x16 或 32x32 瓦片）
- 相机跟随玩家位置，只渲染可见区域内的Chunks（视锥裁剪）
- 支持多图层渲染：地面层、遮挡层、装饰层、遮挡角色层（类似传统传奇的遮挡效果）

**地图数据结构**（与服务端共享）：

```text
MapData
├── map_id: u32          // 地图ID
├── width: u32           // 地图宽度（瓦片数）
├── height: u32          // 地图高度（瓦片数）
├── tile_size: u32       // 单瓦片像素大小
├── layers: Vec<Layer>    // 地图图层
│   ├── layer_id: u8     // 图层ID（决定渲染顺序）
│   ├── tiles: Vec<u32>  // 瓦片ID数组
│   └── opacity: f32    // 图层透明度
├── walk_grid: Vec<bool> // 阻挡数据（true=可通行）
├── objects: Vec<MapObject> // 地图对象（门/花/树等装饰）
└── spawn_points: Vec<SpawnPoint> // 出生点/传送点
```

**遮挡处理**：传奇的遮挡效果通过多层瓦片和透明度实现。当角色A在角色B下方（Y轴更小）时，角色A先渲染，角色B后渲染，形成站立遮挡效果。Bevy的2D渲染顺序通过Z轴深度控制，Y轴较小的实体自动分配较小的Z值。

#### 2.3.2 网络通信模块

**技术实现**：

- 使用 **tokio** 处理异步网络IO（非Bevy内置）
- TCP连接通过 **tokio::net::TcpStream**
- WebSocket支持通过 **tokio-tungstenite**（用于Web平台）
- Protobuf序列化使用 **prost** crate

**网络层设计**：

```
NetworkManager
├── connection_state: ConnectionState    // 未连接/登录中/选服中/游戏中
├── tcp_stream: Option<TcpStream>        // TCP连接
├── send_queue: Channel<OutgoingMessage> // 发送队列
├── recv_handler: Task                   // 接收处理协程
├── heartbeat_timer: Interval            // 心跳定时器
└── reconnect_policy: ReconnectPolicy    // 重连策略

ConnectionState:
  Disconnected → LoggingIn → SelGate → InGame
```

**消息处理流程**：

1. 收到服务端消息字节流
2. 按协议格式解析（包头+长度+类型+序列号+消息体+校验）
3. 将消息反序列化为对应Protobuf结构体
4. 发送到Bevy的事件总线（EventWriter<ServerMessage>）
5. 游戏逻辑系统监听事件并响应

**断线重连**：客户端维护连接状态机，断线后自动按策略重连（3次，间隔递增）。重连成功后发送恢复会话请求，服务端返回当前游戏状态。

#### 2.3.3 玩家表现模块

**角色渲染**：

- 每个玩家实体对应一个 Bevy **Entity**
- 使用 **Sprite** 组件渲染角色精灵图
- 精灵图按职业和方向（8方向）切分
- 站立/行走/攻击/施法/死亡各有独立动画状态

**动画状态机**：

```
AnimationState
├── idle       // 站立待机
├── walk       // 行走
├── run        // 奔跑
├── attack_1   // 普通攻击
├── attack_2   // 技能攻击（不同技能不同动画）
├── spell      // 法师/道士施法
├── hurt       // 受击
├── die        // 死亡
└── sit        // 休息/打坐
```

**移动表现**：

- 客户端本地预测移动（先行平滑位移）
- 服务端校正：定期收到服务端位置同步后修正本地预测
- 平滑插值：使用 lerp（线性插值）在两帧服务端位置之间平滑过渡
- 攻击动画锁定：普攻/技能释放期间，角色朝向锁定目标

#### 2.3.4 特效与音效模块

**视觉特效**：

- 技能特效、爆炸效果使用 **Sprite Animation**（精灵序列帧动画）
- 地面光效（魔法盾、治愈光环）使用 **Sprite** 叠加在角色上
- 屏幕特效（药水效果、BUFF图标）使用 **UI层** 渲染

**音效系统**：

- 使用 **kira** 音频库（纯Rust，支持BGM/SE/3D音效）
- 背景音乐循环播放
- 音效根据事件触发（攻击命中、技能释放、怪物死亡、拾取物品）
- 3D音效根据声源与角色距离和方向计算音量衰减

### 2.4 客户端egui管理界面（开发调试用）

客户端同样提供egui调试界面，方便开发期间查看网络状态和游戏数据：

| 界面区块 | 显示内容 |
|---------|---------|
| 网络状态 | 连接地址、当前状态、延迟、发送/接收字节数、消息队列长度 |
| 角色信息 | 当前角色ID、名称、职业、等级、坐标、HP/MP百分比 |
| 属性面板 | 全部属性数值（DC/MC/SC/AC/MAC/命中/闪避等） |
| 背包 | 背包格子物品列表、仓库物品列表 |
| 附近实体 | 视野内所有玩家、怪物、NPC的ID和坐标 |
| 调试开关 | 显示网格、显示坐标、显示碰撞区域、显示帧率 |
| 控制台 | 日志输出窗口，可输入GM指令（调试用） |

---

## 第三章：经典传奇三职业体系

### 3.1 三大职业定位

| 职业 | 定位 | 核心特征 | 代表技能 |
|------|------|---------|---------|
| **战士** | 近战物理输出 | 高血量、高防御、近身战斗、单体爆发 | 烈火剑法、雷霆一击、逐日剑法、狮子吼 |
| **法师** | 远程魔法输出 | 高魔法攻击、低血量、低防御、范围伤害 | 冰咆哮、火墙、雷电术、流星火雨 |
| **道士** | 辅助与召唤 | 中等属性、召唤骷髅/神兽、治疗、施毒 | 灵魂火符、召唤骷髅、治愈术、噬血术 |

### 3.2 职业属性成长表

| 属性 | 战士 | 法师 | 道士 |
|------|------|------|------|
| 每级HP增长 | 15-20 | 8-10 | 12-15 |
| 每级MP增长 | 5-8 | 15-20 | 10-15 |
| 每级属性点（可自由分配） | 1点 | 1点 | 1点 |
| 初始DC | 5-8 | 1-3 | 1-3 |
| 初始MC | 0 | 8-12 | 0 |
| 初始SC | 0 | 0 | 3-5 |
| 初始AC | 2-4 | 0-1 | 1-2 |
| 初始MAC | 0-2 | 3-6 | 2-4 |
| 初始准确 | 7 | 5 | 8 |
| 初始敏捷 | 5 | 6 | 7 |
| 初始防御 | 2-4 | 0-1 | 1-2 |
| 初始魔御 | 0-2 | 3-6 | 2-4 |
| 初始攻击速度 | 快 | 中 | 中 |
| 初始负重 | 高 | 低 | 中 |
| 初始毒物躲避 | 低 | 低 | 高 |

### 3.3 职业限制

| 限制项 | 战士 | 法师 | 道士 |
|--------|------|------|------|
| 可穿戴装备类型 | 刀/剑/甲/盔/镯/戒/项链 | 魔杖/书/法袍/盔/镯/戒/项链 | 刀/剑/毒/甲/盔/镯/戒/项链 |
| 可学习技能系 | 剑术系 | 冰系/火系/雷系 | 符咒系/召唤系/治愈系 |
| 初始地图 | 比奇省 | 比奇省 | 比奇省 |
| 性别选择 | 男/女 | 男/女 | 男/女 |

### 3.4 三职业技能树

#### 战士技能

| 技能名称 | 技能类型 | 技能等级 | MP消耗 | 冷却时间 | 效果说明 |
|---------|---------|---------|--------|---------|---------|
| 基础剑术 | 被动 | 0级初始 | 0 | — | 永久提升普攻伤害 |
| 攻杀剑术 | 被动 | 3级 | 0 | — | 普攻时有概率打出双倍伤害 |
| 烈火剑法 | 主动 | 7级 | 14 | 10秒 | 瞬间2次高额火属性攻击 |
| 雷霆一击 | 主动 | 15级 | 16 | 12秒 | 范围攻击，对周围目标造成伤害 |
| 逐日剑法 | 主动 | 25级 | 28 | 15秒 | 远程直线突刺，最多攻击4个目标 |
| 狮子吼 | 主动 | 26级 | 12 | 15秒 | 使周围怪物眩晕若干秒（对玩家无效） |
| 野蛮冲撞 | 主动 | 27级 | 10 | 8秒 | 将前方目标撞退2格 |
| 开天斩 | 主动 | 35级 | 35 | 20秒 | 强力斩击，对直线目标造成大量伤害 |

#### 法师技能

| 技能名称 | 技能类型 | 技能等级 | MP消耗 | 冷却时间 | 效果说明 |
|---------|---------|---------|--------|---------|---------|
| 治愈术 | 主动 | 7级 | 8+等级 | 5秒 | 治疗自身或队友（道士可学，部分效果相同） |
| 雷电术 | 主动 | 17级 | 21 | 3秒 | 单体雷属性魔法攻击，高伤害 |
| 冰咆哮 | 主动 | 19级 | 30 | 5秒 | 范围魔法攻击，以自身为中心3x3范围 |
| 火墙 | 主动 | 22级 | 26+ | 持续 | 在地面放置火墙，持续造成伤害 |
| 地狱雷光 | 主动 | 26级 | 42 | 8秒 | 以自身为中心5x5范围雷属性攻击 |
| 流星火雨 | 主动 | 35级 | 60 | 12秒 | 大范围火属性攻击（11x11），高伤害 |
| 魔法盾 | 主动 | 31级 | 50+ | — | 吸收一定量伤害的护盾，持续消耗MP |
| 灭天火 | 主动 | 38级 | 48 | 6秒 | 对单体高火属性魔法伤害，附带灼烧效果 |

#### 道士技能

| 技能名称 | 技能类型 | 技能等级 | MP消耗 | 冷却时间 | 效果说明 |
|---------|---------|---------|--------|---------|---------|
| 灵魂火符 | 主动 | 18级 | 12 | 0.8秒 | 远程道术攻击，伤害与道术高低挂钩 |
| 召唤骷髅 | 主动 | 19级 | 26 | — | 召唤骷髅战士助战（可升级为神兽） |
| 隐身术 | 主动 | 20级 | 22 | — | 隐身，怪物无法主动攻击（攻击或施法后消失） |
| 施毒术 | 主动 | 21级 | 12 | 5秒 | 对目标施放绿毒（持续扣血）或红毒（降低防御） |
| 治愈术 | 主动 | 7级 | 8+等级 | 5秒 | 治疗目标 |
| 集体治愈术 | 主动 | 24级 | 34+ | 10秒 | 对周围队友群体治疗 |
| 召唤神兽 | 主动 | 35级 | 78 | — | 召唤神兽（取代骷髅），攻防更高 |
| 噬血术 | 主动 | 39级 | 37 | 8秒 | 远程道术攻击，伤害的30%转化为自身HP |

---

## 第四章：传奇经典属性体系

### 4.1 属性面板设计

传奇经典属性与主流MMO差异显著，采用独立命名体系：

| 属性名 | 全称 | 说明 | 战士 | 法师 | 道士 |
|--------|------|------|------|------|------|
| **DC** | Damage Class | 物理攻击上限-下限区间 | 高 | 低 | 低 |
| **MC** | Magic Class | 魔法攻击上限-下限区间 | 低 | 高 | 低 |
| **SC** | Spirit Class | 道术攻击上限-下限区间 | 低 | 低 | 高 |
| **AC** | Armor Class | 物理防御上限-下限区间 | 中 | 低 | 中 |
| **MAC** | Magic Armor Class | 魔法防御上限-下限区间 | 低 | 中 | 中 |
| **HP** | Health Points | 生命值 | 高 | 低 | 中 |
| **MP** | Mana Points | 魔法值 | 低 | 高 | 中 |
| **准确** | Accuracy | 物理攻击命中率 | 中 | 低 | 中 |
| **敏捷** | Agility | 影响闪避率和攻击速度 | 中 | 中 | 中 |
| **攻击速度** | Attack Speed | 每秒攻击次数上限 | 高 | 中 | 中 |
| **诅咒** | Curse | 降低目标闪避率 | — | — | 可通过技能提升 |
| **幸运** | Lucky | 提高攻击打出最大伤害的概率 | — | — | — |
| **神圣** | Holy | 对不死系怪物的额外伤害 | 中 | 中 | 中 |
| **中毒躲避** | Poison Resist | 受到毒系伤害减免 | 低 | 低 | 高 |
| **负重** | Weight | 携带物品的最大重量 | 高 | 低 | 中 |

### 4.2 装备属性范围（以沃玛装备为例）

| 装备 | DC | MC | SC | AC | MAC | 其他属性 |
|------|----|----|----|----|-----|---------|
| 炼狱（刀） | 0-25 | — | — | 0 | 0 | 重量85 |
| 魔杖（魔杖） | — | 0-16 | — | 0 | 0 | 重量26 |
| 无极棍（刀） | 8-20 | — | 3-5 | 0 | 0 | 重量50 |
| 魔衣（女） | 0-5 | 0-8 | — | 3-12 | 3-10 | — |

### 4.3 伤害公式详解

#### 4.3.1 战士物理攻击伤害

```
普攻伤害 = random(DC下限, DC上限) - 目标AC/2 + 力量修正 + 武器幸运修正

烈火剑法 = 单次伤害 × 2 × (1 ± 5%随机波动)

逐日剑法 = (第一刀 + 第二刀 + 第三刀 + 第四刀) 总伤害
           每刀 = random(DC下限, DC上限) × 1.2
```

#### 4.3.2 法师魔法攻击伤害

```
魔法伤害 = random(MC下限, MC上限) - 目标MAC/3 + 智力修正 + 技能系数

技能系数：
  雷电术 = 1.0
  冰咆哮 = 0.6（范围分摊）
  火墙 = 0.4（持续）
  流星火雨 = 1.8（高伤害范围）
```

#### 4.3.3 道士道术攻击伤害

```
道术伤害 = random(SC下限, SC上限) - 目标MAC/3 + 精神修正 + 技能系数

技能系数：
  灵魂火符 = 0.8
  噬血术 = 1.2（额外吸血）
  召唤骷髅 = 基础攻击力 × 技能等级 × 0.3（召唤物普攻）
```

#### 4.3.4 暴击与命中

```
普攻最大伤害概率 = (幸运值 / 255) × 100%

物理命中率 = 准确值 / (准确值 + 目标敏捷值) × 基准命中率

物理闪避率 = 目标敏捷值 / (准确值 + 目标敏捷值) × 基准闪避率

暴击伤害 = 当前伤害 × 2.0（固定2倍）
```

### 4.4 防御机制

#### 4.4.1 物理防御（AC）

```
有效防御 = random(AC下限, AC上限)

实际减伤 = min(攻击伤害 × 0.5, 防御效果 × 2)

注：AC越高，减伤效果越明显，但存在最低伤害保底
```

#### 4.4.2 魔法防御（MAC）

```
有效魔御 = random(MAC下限, MAC上限)

实际魔法减伤 = min(魔法伤害 × 0.3, 魔御效果 × 3)

注：MAC对魔法伤害的减免效率低于AC对物理伤害
```

### 4.5 负重与背包

| 项目 | 战士 | 法师 | 道士 |
|------|------|------|------|
| 基础背包负重 | 50 | 30 | 40 |
| 每级负重增长 | +3 | +1 | +2 |
| 装备增加负重上限 | 高 | 低 | 中 |
| 背包容量（格数） | 40 | 40 | 40 |
| 仓库容量 | 80 | 80 | 80 |

当负重超过上限时，角色无法拾取新物品。

### 4.6 经验值曲线（经典传奇）

| 等级 | 升级所需经验 | 累计经验 | 怪物参考 |
|------|------------|---------|---------|
| 1 | 0 | 0 | — |
| 7 | 2,000 | ~5,000 | 鹿/稻草人 |
| 15 | 10,000 | ~40,000 | 沃玛寺庙1-2层 |
| 22 | 50,000 | ~150,000 | 蜈蚣洞 |
| 28 | 150,000 | ~400,000 | 祖玛寺庙 |
| 35 | 500,000 | ~1,200,000 | 赤月峡谷 |
| 40 | 1,500,000 | ~3,000,000 | 牛魔寺庙 |
| 46 | 5,000,000 | ~10,000,000 | 魔龙城 |
| 50 | 10,000,000 | ~30,000,000 | 魔龙沼泽 |

---

## 第五章：M2Server ECS架构设计

### 5.1 ECS架构概述

ECS（Entity Component System）是游戏开发中广泛使用的数据导向架构模式。与传统面向对象架构的对比如下：

| 维度 | 传统面向对象（V3.0） | ECS架构（V4.0） |
|------|--------------------|----------------|
| 实体表示 | 类继承树（Player/Monster继承自Entity） | Entity（简单ID） + Components（数据） |
| 属性存储 | 分散在各实体类中 | 组件数组，连续内存存储 |
| 行为实现 | 方法绑定在类中 | System（纯函数）处理组件数据 |
| 实体创建 | new Player() / new Monster() | world.spawn() + 附加Components |
| 组件添加/删除 | 继承链修改 | 动态附加/移除Components |
| 缓存友好性 | 差（对象分散，引用跳转） | 优（同类组件连续内存访问） |
| 并行处理 | 需手动加锁 | 组件系统天然并行（无共享可变状态） |
| 代码复用 | 继承层次复杂时难以复用 | System可作用于任何匹配Components的Entity |

### 5.2 M2Server ECS架构

```
┌──────────────────────────────────────────────────────────┐
│                     ECS World                              │
│                                                        │
│  ┌─────────────────────────────────────────────────────┐│
│  │                 Components（数据组件）                ││
│  │  Position, Health, Mana, CombatStats, Equipment,    ││
│  │  Inventory, Skills, BuffList, AIMode, TeamInfo,     ││
│  │  GuildRef, Nameplate, WalkPath, Target, WalkSpeed   ││
│  └─────────────────────────────────────────────────────┘│
│                                                        │
│  ┌─────────────────────────────────────────────────────┐│
│  │                 Systems（行为系统）                   ││
│  │                                                        ││
│  │  MovementSystem      → 处理移动/寻路/位置同步          ││
│  │  CombatSystem        → 处理攻击判定/伤害结算            ││
│  │  SkillSystem         → 处理技能释放/效果/冷却           ││
│  │  BuffSystem          → 处理BUFF/DEBUFF定时器          ││
│  │  MonsterAISystem      → 处理怪物AI状态机               ││
│  │  InventorySystem     → 处理物品增删改查/装备穿戴       ││
│  │  DeathSystem         → 处理死亡/掉落/复活              ││
│  │  RespawnSystem       → 处理怪物刷新/玩家复活           ││
│  │  LootSystem          → 处理掉落判定/物品生成           ││
│  │  TeamSystem          → 处理组队/经验分配              ││
│  │  ChatSystem          → 处理聊天/消息广播               ││
│  │  SyncSystem          → 生成视野同步消息推送给客户端    ││
│  │  ScriptSystem        → Lua脚本触发与回调               ││
│  │  ScheduleSystem      → 定时任务调度                    ││
│  └─────────────────────────────────────────────────────┘│
│                                                        │
│  ┌─────────────────────────────────────────────────────┐│
│  │                 Entity Builder                       ││
│  │  spawn_player() → 附加 PlayerTag+Position+HP+Mana... ││
│  │  spawn_monster() → 附加 MonsterTag+AIMode+DropTable ││
│  │  spawn_npc() → 附加 NPCTag+DialogScriptID           ││
│  └─────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────┘
                    │
                    │ 每帧Tick循环（固定频率，如每秒10帧）
                    ▼
┌──────────────────────────────────────────────────────────┐
│                    Schedule Loop                          │
│                                                        │
│  1. 接收消息队列（来自RunGate的玩家操作消息）              │
│  2. 将消息转化为Component变更或Event                      │
│  3. 按顺序执行所有Systems（部分Systems可并行）             │
│  4. 生成同步事件（SyncEvent → 推送给RunGate）            │
│  5. 处理定时器到期事件（怪物刷新/活动开启）                │
│  6. 定期保存玩家数据（每N分钟）                           │
└──────────────────────────────────────────────────────────┘
```

### 5.3 Components（组件）定义

每个Component是纯数据结构，不包含任何行为逻辑。

#### 基础组件

```text
PositionComponent
  - x: i32            // 当前X坐标（像素）
  - y: i32            // 当前Y坐标（像素）
  - target_x: i32     // 目标X坐标（移动中）
  - target_y: i32     // 目标Y坐标（移动中）
  - map_id: u32       // 所在地图ID
  - direction: u8     // 朝向（0-7，8方向）

HealthComponent
  - hp: i32           // 当前生命值
  - max_hp: i32       // 最大生命值
  - regen_hp: i32     // 每秒自然回血量

ManaComponent
  - mp: i32           // 当前魔法值
  - max_mp: i32       // 最大魔法值
  - regen_mp: i32     // 每秒自然回蓝量

CombatStatsComponent
  - dc_min: i32       // 攻击下限（DC下限）
  - dc_max: i32       // 攻击上限（DC上限）
  - mc_min: i32       // 魔法攻击下限
  - mc_max: i32       // 魔法攻击上限
  - sc_min: i32       // 道术攻击下限
  - sc_max: i32       // 道术攻击上限
  - ac_min: i32       // 防御下限
  - ac_max: i32       // 防御上限
  - mac_min: i32      // 魔御下限
  - mac_max: i32      // 魔御上限
  - accuracy: i32     // 准确
  - agility: i32      // 敏捷
  - lucky: i32        // 幸运
  - holy: i32         // 神圣
  - walk_speed: i32   // 移动速度
  - attack_speed: i32 // 攻击速度
```

#### 实体标识组件

```text
PlayerTag           // 标识：这是一个玩家实体
MonsterTag          // 标识：这是一个怪物实体
NPCTag              // 标识：这是一个NPC实体
DroppedItemTag      // 标识：这是一个掉落的物品
EffectTag           // 标识：这是一个技能特效实体
PetTag              // 标识：这是一个宠物/召唤物实体
```

#### 玩家特有组件

```text
PlayerInfoComponent
  - account_id: u64
  - char_id: u64
  - name: String
  - job: JobType      // 战士/法师/道士
  - level: u32
  - exp: u64
  - gender: Gender    // 男/女

InventoryComponent
  - slots: Vec<Option<InventorySlot>>  // 背包格子
  - capacity: u32

EquipmentComponent
  - slots: [Option<EquipmentItem>; 12] // 12个装备栏位
  // 武器/衣服/头盔/项链/勋章/护腕0/护腕1/戒指0/戒指1/腰带/鞋子/披风

SkillListComponent
  - skills: Vec<SkillRecord>  // 已学会的技能

BuffListComponent
  - buffs: Vec<ActiveBuff>      // 当前生效的BUFF列表

TeamInfoComponent
  - team_id: Option<u64>       // 队伍ID
  - exp_share_type: ShareType  // 经验分配方式

GuildRefComponent
  - guild_id: Option<u64>
  - guild_rank: GuildRank

WalkPathComponent
  - path: Vec<(x, y)>          // 当前寻路路径
  - path_index: usize          // 路径走到第几步
```

#### 怪物特有组件

```text
MonsterInfoComponent
  - monster_config_id: u32     // 怪物配置ID
  - monster_type: MonsterType  // 普通/精英/BOSS

AIStateComponent
  - current_state: AIState     // Idle/Patrol/Chase/Attack/Return/Dead
  - target_entity: Option<EntityId> // 当前仇恨目标
  - hate_table: HashMap<EntityId, i32> // 仇恨表
  - patrol_path: Vec<(x,y)>   // 巡逻路径
  - alert_range: i32          // 警戒范围（像素）
  - chase_range: i32           // 追击范围

DropTableComponent
  - drop_group_id: u32        // 掉落组ID
  - drop_rate_multiplier: f32 // 掉落倍率（可配置）

RespawnComponent
  - respawn_time: u64          // 复活时间戳
  - spawn_x: i32               // 出生X
  - spawn_y: i32               // 出生Y
```

### 5.4 Systems（系统）设计

每个System是纯函数，接收World引用，查询匹配条件的Entities，执行数据变更。不包含任何业务数据存储。

#### MovementSystem

```text
职责：处理所有实体的移动逻辑

处理逻辑：
1. 查询所有拥有 PositionComponent + (PlayerTag | MonsterTag) 的实体
2. 对于每个实体：
   a. 若有WalkPathComponent：
      - 沿路径前进一格（根据WalkSpeed计算是否到时间）
      - 若到达路径终点，清空WalkPathComponent
   b. 若无WalkPath但有target_position：
      - 重新计算寻路路径（调用A*寻路）
   c. 若无任何移动目标：
      - 不处理
3. 处理地图边界和阻挡检测（不可通行则停止移动）
4. 触发SyncEvent（位置变化推送给客户端）

性能优化：
- 寻路计算每帧最多处理N个实体（N可配置）
- 大量实体同时寻路时排队处理
```

#### CombatSystem

```text
职责：处理所有战斗相关逻辑

处理逻辑：
1. 查询所有拥有 CombatStatsComponent + HealthComponent 的实体
2. 处理每帧到期的攻击判定：
   a. 验证目标是否有效（存在、同一地图、距离合法）
   b. 根据实体职业计算伤害（DC/MC/SC → 伤害公式）
   c. 应用目标AC/MAC防御计算
   d. 计算暴击、命中、闪避
   e. 扣除目标HP
   f. 判断目标是否死亡（HP <= 0）
3. 死亡判定后：
   a. 触发DeathSystem处理死亡逻辑
   b. 触发LootSystem处理掉落
   c. 分配经验给攻击者和队伍成员
4. 广播战斗结果（伤害数字显示、死亡效果）

关键设计：
- 战斗逻辑完全数据驱动，伤害公式通过配置表定义
- 无需修改代码即可调整数值平衡
```

#### MonsterAISystem

```text
职责：驱动怪物的AI状态机

状态机定义：
┌─────────┐
│  Idle   │ ← 待机，每帧判断是否进入巡逻或追击
└────┬────┘
     │ 检测到目标进入警戒范围
     ▼
┌─────────┐
│ Patrol  │ ← 沿巡逻路径移动，到达终点返回起点
└────┬────┘
     │ 目标进入攻击范围
     ▼
┌─────────┐
│ Chase   │ ← 向目标移动（追击）
└────┬────┘
     │ 目标离开追击范围 或 自身HP过低
     ▼
┌─────────┐
│ Return  │ ← 返回出生点
└────┬────┘
     │ 到达出生点
     ▼
┌─────────┐
│  Idle   │
└─────────┘

┌─────────┐
│ Attack  │ ← 攻击范围内目标（独立于其他状态）
└─────────┘
```

#### SkillSystem

```text
职责：处理技能释放、冷却、效果

技能释放流程（通过事件触发）：
1. 接收 SkillCastEvent（施法者Entity + 技能ID + 目标Entity/坐标）
2. 验证施法条件：
   a. 技能是否已学习
   b. MP是否足够
   c. 冷却是否结束
   d. 目标是否在有效范围内
   e. 施法者状态是否允许施法（不能死亡/眩晕中）
3. 扣除MP，启动冷却计时器
4. 触发技能效果：
   a. 即时效果：直接应用伤害/治疗
   b. 持续效果：附加BuffComponent
   c. 召唤效果：在目标位置生成PetTag实体
   d. 特殊效果：触发Lua脚本
5. 发送施法成功事件（用于客户端播放动画）
```

#### BuffSystem

```text
职责：管理所有BUFF/DEBUFF的定时效果

处理逻辑：
1. 每帧检查所有BuffListComponent
2. 对每个ActiveBuff：
   a. 递减剩余时间
   b. 若到达间隔触发时间：执行间隔效果（如周期性扣血）
   c. 若时间归零：移除Buff，触发移除效果（恢复属性等）
3. 排序BUFF优先级（某些BUFF不可叠加，取最高）

BUFF类型枚举：
  - 属性加成型（BUFF增加DC/AC/MAC等）
  - 持续伤害型（每秒扣血，如中毒）
  - 持续治疗型（每秒回血）
  - 控制型（眩晕/冰冻/沉默/定身）
  - 隐身型（客户端显示隐身）
  - 召唤型（召唤物存在期间）
```

### 5.5 ECS架构的实战优势

#### 5.5.1 性能优势示例：怪物AI并行处理

传统面向对象架构中，每个怪物是一个对象，每个对象的Update方法可能相互影响（读取/修改共享状态），必须加锁。

ECS架构下，MonsterAISystem作为单次遍历处理所有怪物：

```
// 伪代码示意（实际为Rust代码）
fn update_monster_ai(world: &mut World) {
    // 一次性读取所有怪物数据（连续内存）
    let monsters: Vec<(Entity, &Position, &AIState)> = 
        world.query::<(&Position, &AIState)>()  // Archetype查询，高效
             .with::<MonsterTag>()
             .iter()
             .collect();
    
    // 遍历所有怪物，各自独立处理（无共享可变状态）
    for (entity, position, ai_state) in monsters {
        let new_state = compute_next_state(entity, position, ai_state);
        // 修改组件数据
        world.get_mut::<AIState>(entity).unwrap().current_state = new_state;
    }
}
```

所有怪物在同一帧的处理过程中，**无任何共享可变状态**，天然支持多线程并行。只需将monsters数组分片，分配给不同线程处理，即可实现零锁并行AI计算。

#### 5.5.2 内存布局优势

ECS架构的组件按类型存储，而非按实体存储：

```
// 传统OOP（每个Entity独立内存）
Entity[0]: HP=100, AC=5,  Name="玩家A" ...
Entity[1]: HP=200, AC=10, Name="玩家B" ...  ← 不连续内存，CPU缓存miss多

// ECS（同类组件连续内存）
HP:   [100, 200, 150, 300, ...]  ← 连续内存，一次读取多个
AC:   [5,   10,  7,   15,   ...]  ← 连续内存
Name: ["玩家A", "玩家B", ...]     ← 字符串单独区域
```

当CombatSystem处理AC（防御）时，只需访问AC组件的连续内存块，CPU缓存命中率极高，性能提升显著。

#### 5.5.3 实体类型扩展优势

假设需要新增"宠物"实体类型，传统OOP需要新建Pet类继承Entity，或修改基类。ECS架构只需：

```rust
// 创建宠物实体 = 选择性附加组件
let pet = world.spawn();
world.insert(pet, (
    PlayerInfoComponent { /* ... */ },
    HealthComponent { hp: 500, max_hp: 500, regen_hp: 0 },
    CombatStatsComponent { dc_min: 10, dc_max: 20, /* ... */ },
    PositionComponent { x: 100, y: 200, /* ... */ },
    MonsterTag,  // 复用MonsterTag使AI系统也驱动宠物
    PetTag,      // 额外标识这是宠物
    WalkPathComponent { /* 宠物跟随主人的路径 */ },
));
```

无需修改任何现有System，Pet就具备了所有需要的行为（移动、战斗、AI）。这就是ECS的**组合优于继承**原则。

### 5.6 ECS与Lua脚本的集成

Lua脚本用于处理无法预定义的复杂业务逻辑（如任务流程、特殊活动、NPC对话分支），但脚本只调用Rust暴露的API，不直接操作ECS。

```
┌─────────────────────────────────────────────────────────────┐
│  Lua脚本（处理复杂业务逻辑）                                   │
│  例：任务脚本、特殊活动脚本、NPC对话分支                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  script_quest.lua                                    │   │
│  │  function on_accept_quest(player, quest_id)           │   │
│  │    api.give_item(player, "任务道具A", 1)              │   │
│  │    api.set_flag(player, "已完成新手任务")               │   │
│  │  end                                                 │   │
│  └─────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────┘
                             │ mlua FFI调用
                             ▼
┌─────────────────────────────────────────────────────────────┐
│  Rust API层（Lua ↔ ECS 桥梁）                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  LuaAPI                                               │   │
│  │  - give_item(char_id, item_config_id, count)          │   │
│  │  - take_item(char_id, item_config_id, count)          │   │
│  │  - teleport(char_id, map_id, x, y)                    │   │
│  │  - give_exp(char_id, exp_amount)                      │   │
│  │  - send_message(char_id, message)                     │   │
│  │  - get_var(char_id, var_name) → value                 │   │
│  │  - set_var(char_id, var_name, value)                 │   │
│  └──────────────────────────────────────────────────────┘   │
│  每个API函数内部通过char_id查询ECS World中的Entity，           │
│  修改对应Components（如InventoryComponent、PositionComponent）│
└─────────────────────────────────────────────────────────────┘
```

---

## 第六章：客户端与ECS的同构设计

### 6.1 客户端Bevy ECS

Bevy引擎原生基于ECS架构，客户端与M2Server使用相同的ECS概念：

| 层级 | M2Server | 客户端（Bevy） |
|------|---------|---------------|
| 框架 | 自主ECS（bevy_ecs兼容API） | **bevy_ecs** 原生 |
| 实体 | EntityId（u64） | Entity（bevy_ecs原生） |
| 数据 | Components（Rust struct） | Components（Rust struct） |
| 行为 | Systems（Rust函数） | Systems（Rust函数 + bevy_plugin） |
| 世界 | World | World |

### 6.2 客户端Components设计

客户端复用与服务端相同的Components定义（通过共享crate），同时增加客户端特有的组件：

```text
// 共享组件（服务端+客户端共用）
PositionComponent
CombatStatsComponent
HealthComponent
PlayerInfoComponent
SkillListComponent

// 客户端特有组件
SpriteSheetComponent      // 精灵图渲染配置
AnimationStateComponent    // 当前动画状态和帧索引
LocalPlayerTag             // 标识当前操控的玩家（本地）
RemotePlayerTag            // 标识其他玩家（远程同步）
PendingMovementComponent   // 本地预测移动（待服务端确认）
InterpolateComponent       // 插值参数（用于平滑同步）
EffectEntityComponent      // 关联服务端特效Entity的客户端表现实体
UIContextComponent         // 当前UI状态（背包打开/对话中/交易中）
```

### 6.3 客户端Systems设计

```text
// 客户端Systems
InputSystem          → 收集键盘/鼠标输入，转换为Intent事件
MovementSystem       → 处理本地玩家移动预测，发送移动请求
AnimationSystem      → 根据动画状态更新精灵图帧
InterpolationSystem  → 对远程玩家/怪物位置做插值平滑
SyncSystem           → 接收服务端消息，更新本地ECS组件
UIRenderSystem       → 根据UI状态渲染对应界面
EffectSystem         → 根据服务端事件播放特效（爆炸/光环/buff图标）
AudioSystem          → 根据事件播放音效
CameraSystem         → 相机跟随本地玩家
```

### 6.4 共享结构定义（代码复用）

```
mir2-shared/               ← 共享crate（服务端+客户端都依赖）
├── proto/
│   ├── player.proto        → Protobuf定义（账号/角色/物品等）
│   ├── map.proto           → 地图数据结构
│   ├── skill.proto         → 技能定义
│   ├── combat.proto        → 战斗协议
│   └── chat.proto          → 聊天协议
├── constants/
│   ├── skill_data.rs       → 技能配置常量
│   ├── item_data.rs        → 物品配置常量
│   └── map_data.rs         → 地图配置常量
├── components/              ← ECS Components（服务端+客户端共用）
│   ├── position.rs
│   ├── combat_stats.rs
│   ├── health.rs
│   └── ...
└── calc/
    ├── damage_formula.rs   → 伤害公式（服务端用，客户端演示用）
    ├── drop_formula.rs     → 掉落公式
    └── exp_formula.rs      → 经验公式
```

**共享crate的优势**：

- 协议定义只写一次，服务端和客户端保持100%一致
- 伤害公式代码在服务端和客户端各有一份（客户端用于本地演示，服务端为权威）
- 物品配置、怪物配置、地图配置等常量共享定义
- 修改配置时只需改一处，编译时两端同时生效

---

## 第七章：服务间通信协议（V4.0更新）

### 7.1 协议序列化选型：Protobuf

V4.0将服务间通信和客户端-服务端通信统一使用 **Protocol Buffers**（通过prost crate）。

**选择理由**：

| 对比项 | 自研二进制 | Protobuf | FlatBuffers | Cap'n Proto |
|-------|-----------|----------|-------------|-------------|
| 跨语言 | 需手写编解码 | 原生支持Rust/C++/Java/Go等 | 原生支持 | 原生支持 |
| 工具链 | 无 | 成熟（protoc编译器）| 成熟 | 成熟 |
| 性能 | 最优（手写） | 优 | 极高（零拷贝）| 极高 |
| 版本兼容 | 需手写兼容 | 自动兼容 | 自动兼容 | 自动兼容 |
| 学习成本 | 高（需设计格式）| 低 | 中 | 中 |
| 代码生成 | 无 | 自动生成Rust/C++代码 | 自动生成 | 自动生成 |

### 7.2 Protobuf消息定义示例

```protobuf
// 玩家相关
message PlayerData {
  uint64 char_id = 1;
  string name = 2;
  JobType job = 3;
  uint32 level = 4;
  uint64 exp = 5;
  uint32 map_id = 6;
  int32 x = 7;
  int32 y = 8;
  Gender gender = 9;
  CombatStats stats = 10;
  repeated InventoryItem inventory = 11;
  repeated EquipmentItem equipment = 12;
  repeated SkillRecord skills = 13;
}

// 战斗相关
message CombatStats {
  int32 dc_min = 1;
  int32 dc_max = 2;
  int32 mc_min = 3;
  int32 mc_max = 4;
  int32 sc_min = 5;
  int32 sc_max = 6;
  int32 ac_min = 7;
  int32 ac_max = 8;
  int32 mac_min = 9;
  int32 mac_max = 10;
  int32 hp = 11;
  int32 max_hp = 12;
  int32 mp = 13;
  int32 max_mp = 14;
  int32 accuracy = 15;
  int32 agility = 16;
  int32 lucky = 17;
}

// 技能相关
message SkillRecord {
  uint32 skill_id = 1;
  uint32 level = 2;
  uint64 cooldown_expire = 3; // 冷却到期时间戳
}

// 物品相关
message InventoryItem {
  uint64 item_uid = 1;
  uint32 config_id = 2;
  uint32 count = 3;
  uint32 durability = 4;
  repeated ItemOption options = 5; // 强化/追加属性
}

// 服务端 → 客户端同步
message S2CEntityUpdate {
  repeated PositionSync positions = 1;
  repeated HPMPChange hp_mp_changes = 2;
  repeated SkillCast skill_casts = 3;
  repeated EffectShow effects = 4;
  repeated BuffApply buffs = 5;
}
```

---

## 第八章：部署架构与运维（继承V3.0）

### 8.1 部署形态

V4.0的部署方案与V3.0保持一致，每个服务为独立可执行文件：

| 机器 | 运行服务 | 资源需求 |
|------|---------|---------|
| 网关机器 | LoginGate + SelGate + RunGate | 带宽要求高，CPU中等 |
| 引擎机器 | M2Server | CPU和内存要求最高 |
| 数据机器 | DBServer + MySQL + Redis | 内存和IO要求高 |
| 管理机器 | LoginSrv + LogServer + GameCenter | 资源要求最低 |

### 8.2 服务启动顺序（与V3.0一致）

```
LogServer → DBServer → GameCenter → LoginSrv → M2Server 
→ RunGate → SelGate → LoginGate
```

### 8.3 构建工具链

| 工具 | 版本要求 | 用途 |
|------|---------|------|
| rustc | 1.75+ | Rust编译器 |
| cargo | 最新稳定版 | 包管理和构建 |
| protoc | 3.x+ | Protobuf代码生成 |
| cargo-make | 最新 | 任务编排（批量构建、代码生成）|

**一键构建脚本**（cargo-make.toml）：

```text
[tasks.build-all]
dependencies = ["generate-proto", "build-server", "build-client"]

[tasks.generate-proto]
command = "protoc"
args = ["-I./proto", "--prost_out=./shared/src", "--rust_out=./shared/src", "*.proto"]

[tasks.build-server]
command = "cargo"
args = ["build", "--release", "-p", "server"]

[tasks.build-client]
command = "cargo"
args = ["build", "--release", "-p", "client"]

[tasks.run-game]
dependencies = ["build-all"]
command = "cargo"
args = ["run", "-p", "game_center"]  # 或单独运行各服务
```

---

## 第九章：实施路线图

### 9.1 开发阶段划分

| 阶段 | 时长 | 目标 | 里程碑 |
|------|------|------|-------|
| **阶段一：基础架构** | 3周 | 8服务框架 + ECS基础 + Bevy项目 | 每个服务可独立启动，客户端可显示空白窗口 |
| **阶段二：数据层+登录** | 4周 | DBServer + 登录流程全通 + 选服全通 | 可创建角色并进入游戏空地图 |
| **阶段三：ECS核心逻辑** | 6周 | ECS框架完善 + 地图加载 + 移动系统 + 战斗系统 | 玩家可在地图内自由移动并战斗 |
| **阶段四：Bevy客户端渲染** | 5周 | 瓦片地图渲染 + 角色渲染 + 动画系统 + 网络同步 | 客户端视觉效果达到可玩水平 |
| **阶段五：技能与物品** | 4周 | 三职业技能全实现 + 物品系统 + 背包装备 | 三大职业技能均可正常使用 |
| **阶段六：怪物与AI** | 3周 | 怪物刷新 + 完整AI状态机 + 掉落系统 | 地图怪物正常刷新，可掉落物品 |
| **阶段七：社交与行会** | 3周 | 聊天 + 组队 + 行会 + 沙巴克 | 社交功能完整 |
| **阶段八：GM与运维** | 2周 | GM指令集 + GameCenter完善 + 监控 + 备份 | 运营工具完整 |
| **阶段九：测试与调优** | 3周 | 功能测试 + 压力测试 + 性能优化 | 达到上线标准 |
| **总计** | **33周（约8个月）** | — | — |

### 9.2 团队配置

| 角色 | 人数 | 职责 |
|-----|------|------|
| 技术总监 | 1 | 架构决策、技术难点攻关 |
| ECS/Rust专家 | 1 | ECS框架设计、M2Server核心开发 |
| 服务端开发 | 2-3 | 各服务开发（网关/登录/DB/日志/管理） |
| Bevy/客户端开发 | 2-3 | Bevy渲染、网络同步、UI实现 |
| 数值策划 | 1 | 数值体系设计、公式配置 |
| 系统策划 | 1 | 玩法设计、功能需求 |
| 测试工程师 | 1-2 | 功能测试、压测 |
| 运维工程师 | 1 | 部署、监控、备份 |
| **最小团队** | **9-12人** | — |

### 9.3 关键里程碑

| 里程碑 | 时间 | 验收标准 |
|-------|------|---------|
| **M1：可运行骨架** | 第3周 | 8个服务均可启动，egui界面正常，客户端Bevy窗口可显示 |
| **M2：登录可玩** | 第7周 | 完整注册→登录→选服→进入游戏流程可用 |
| **M3：战斗基础** | 第13周 | 移动/普攻/受击/死亡/复活完整可用，伤害公式正确 |
| **M4：技能完整** | 第17周 | 战/法/道三职业全部技能可释放，效果正确 |
| **M5：完整游戏循环** | 第23周 | 练级→打怪→掉落→拾取→装备→变强 完整循环可用 |
| **M6：上线标准** | 第33周 | 500人在线稳定运行，GM工具完备，数据安全 |

---

## 第十章：V4.0技术选型总结与最终立场

### 10.1 最终技术选型总表

| 技术维度 | 选型 | 版本/备注 |
|---------|------|---------|
| 服务端语言 | Rust | 1.75+ |
| 客户端语言 | Rust | 与服务端统一 |
| 服务端游戏逻辑架构 | **ECS** | 自主实现（参考bevy_ecs API） |
| 客户端游戏引擎 | **Bevy** | 最新稳定版 |
| 异步运行时 | tokio | 服务端+客户端网络层共用 |
| 服务端界面 | egui | 每个服务独立egui窗口 |
| 协议序列化 | **Protobuf** | prost |
| 数据库 | MySQL 8.0+ | 结构化数据 |
| 缓存/会话 | Redis 7.0+ | 在线状态、令牌、缓存 |
| 脚本引擎 | Lua 5.4 | mlua集成 |
| 服务间通信 | Protobuf二进制 | 轻量自研 |
| 客户端-服务端协议 | Protobuf二进制 | 与服务间通信统一 |

### 10.2 V4.0相比V3.0的核心升级

| 维度 | V3.0 | V4.0 |
|------|------|------|
| **客户端引擎** | Unity | **Bevy** |
| **客户端语言** | C# | **Rust** |
| **M2Server架构** | 面向对象模块 | **ECS数据导向** |
| **职业体系** | 未定义 | **战/法/道三职业** |
| **属性体系** | 通用MMO属性 | **传奇经典DC/MC/SC/AC/MAC** |
| **协议格式** | 自研二进制 | **Protobuf** |
| **技术栈统一性** | 服务端Rust | **服务端Rust + 客户端Bevy/Rust** |

### 10.3 核心设计立场

**立场一：ECS是M2Server的必然选择。** 传奇类MMO的核心挑战是大规模实体（数千玩家+怪物+NPC）的并发处理。ECS的数据局部性、缓存友好性、无锁并行能力，是应对这一挑战的最佳架构。面向对象架构在5000人规模下将面临严重的性能瓶颈和并发锁竞争。

**立场二：Bevy是Rust游戏客户端的最优解。** 虽然Bevy的社区不如Unity成熟，但其纯Rust技术栈、ECS同构性、无GC性能、无商业锁定等优势，使其成为本项目的最佳选择。客户端的渲染质量和稳定性（2D游戏场景下）完全可满足传奇类游戏需求。

**立场三：技术栈统一是长期投资。** 服务端Rust + 客户端Bevy/Rust + Protobuf共享定义，意味着团队只需要精通一门语言和一个生态。虽然学习曲线较单一Unity方案陡峭，但长期看代码复用率高、技术债务少、团队技能可迁移。

**立场四：经典传奇玩法不可妥协。** 三职业体系（战/法/道）、经典属性（DC/MC/SC/AC/MAC）、经典伤害公式、经典掉落体验——这些是传奇玩家的核心记忆。现代化技术只是手段，玩法体验才是目的。

**立场五：8服务架构保持不变。** 传统8服务架构（LoginGate/SelGate/RunGate/LoginSrv/DBServer/M2Server/LogServer/GameCenter）经二十年运营验证，运维人员熟悉，改动风险高收益低。只改变实现技术，不改变服务划分。

### 10.4 风险提示

| 风险 | 等级 | 应对 |
|-----|------|------|
| Bevy在大规模MMO客户端中缺少商业案例 | 中 | 初期注重核心功能，渲染方案可在后期替换为更成熟的wgpu直接调用 |
| ECS架构对团队有一定学习成本 | 低 | 安排ECS专项培训，bevy_ecs文档完善 |
| Protobuf对实时性要求极高的场景有轻微序列化开销 | 低 | Protobuf序列化/反序列化开销在1ms以内，对游戏逻辑帧（100ms）可忽略 |
| Rust编译时间较长影响开发效率 | 低 | 使用增量编译，合理划分crate，CI/CD预编译 |

---

**文档结束**

**编制日期**：2026年
**文档版本**：V4.0
**归档状态**：已归档
